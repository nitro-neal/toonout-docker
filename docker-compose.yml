version: '3.8'

services:
  toonout:
    build: .
    container_name: toonout-api
    ports:
      - "1337:1337"
    environment:
      - TOONOUT_API_KEY=${TOONOUT_API_KEY:-}  # Optional API key
    restart: unless-stopped
    # Uncomment below for GPU support (requires nvidia-docker)
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
